#include "lists.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * reverse_listint - reverse the list
 * @head: the list
 *
 * Return: the list
 */
listint_t *reverse_listint(listint_t **head)
{
	listint_t temp = *head;
	return (*head);
}
